<div class="container p-5">
  <h1>Add a Movie to <%= @list.name %></h1>

  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.button :submit, class: "btn btn-success mr-3" %>
    <%= link_to "Cancel", list_path(@list), class: "btn btn-warning" %>
  <% end %>


</div>
